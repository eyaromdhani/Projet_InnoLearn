<form method="post" action="{{ path('app_stage_condidature_delete', {'id': stage_condidature.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stage_condidature.id) }}">
    <button class="btn">Delete</button>
</form>
