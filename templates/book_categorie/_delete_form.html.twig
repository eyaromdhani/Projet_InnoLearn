<form method="post" action="{{ path('app_book_categorie_delete', {'id': book_categorie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ book_categorie.id) }}">
    <button class="btn">Delete</button>
</form>
