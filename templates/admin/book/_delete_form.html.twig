<form method="post" action="{{ path('admin_book_delete', {'id': book.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce livre ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ book.id) }}">
    <button class="btn-primary" style="padding: 0.5rem 1rem; border-radius: 6px; background-color: #ef4444; border: none; cursor: pointer; color: white;">Supprimer</button>
</form>
