{% extends 'admin/layout.html.twig' %}

{% block title %}Nouveau Quiz{% endblock %}

{% block admin_content %}
<div class="card animate-fade-in-up">
<<<<<<< HEAD
    <div class="card-header" style="justify-content: space-between; display: flex; align-items: center;">
        <h3 class="card-title">Créer un nouveau quiz</h3>
        <a href="{{ path('admin_quiz_index') }}" class="btn-primary" style="text-decoration: none; padding: 0.5rem 1rem; border-radius: 6px; background-color: #64748b; border: none;">Retour à la liste</a>
    </div>
    <div style="padding: 1.5rem;">
        {{ form_start(form) }}
            <div style="display: grid; gap: 1.5rem; max-width: 800px;">
                <div class="form-group">
                    {{ form_label(form.title, 'Titre', {'label_attr': {'style': 'display: block; margin-bottom: 0.5rem; font-weight: 500; color: #1e293b;'}}) }}
                    {{ form_widget(form.title, {'attr': {'class': 'form-control', 'style': 'width: 100%; padding: 0.75rem; border: 1px solid #e2e8f0; border-radius: 8px;'}}) }}
                    {{ form_errors(form.title) }}
                </div>

                <div class="form-group">
                    {{ form_label(form.difficulty, 'Difficulté', {'label_attr': {'style': 'display: block; margin-bottom: 0.5rem; font-weight: 500; color: #1e293b;'}}) }}
                    {{ form_widget(form.difficulty, {'attr': {'class': 'form-control', 'style': 'width: 100%; padding: 0.75rem; border: 1px solid #e2e8f0; border-radius: 8px;'}}) }}
                    {{ form_errors(form.difficulty) }}
                </div>

                <div class="form-group">
                    {{ form_label(form.duration, 'Durée (minutes)', {'label_attr': {'style': 'display: block; margin-bottom: 0.5rem; font-weight: 500; color: #1e293b;'}}) }}
                    {{ form_widget(form.duration, {'attr': {'class': 'form-control', 'style': 'width: 100%; padding: 0.75rem; border: 1px solid #e2e8f0; border-radius: 8px;'}}) }}
                    {{ form_errors(form.duration) }}
                </div>

                <div class="form-group">
                    {{ form_label(form.description, 'Description', {'label_attr': {'style': 'display: block; margin-bottom: 0.5rem; font-weight: 500; color: #1e293b;'}}) }}
                    {{ form_widget(form.description, {'attr': {'class': 'form-control', 'style': 'width: 100%; padding: 0.75rem; border: 1px solid #e2e8f0; border-radius: 8px;', 'rows': '5'}}) }}
                    {{ form_errors(form.description) }}
                </div>

                <div style="margin-top: 1rem;">
                    <button class="btn-primary" style="padding: 0.75rem 1.5rem; border-radius: 8px; border: none; cursor: pointer;">Enregistrer</button>
                </div>
            </div>
        {{ form_end(form) }}
    </div>
</div>
=======
    <div class="card-header">
        <h3 class="card-title">Créer un nouveau quiz</h3>
    </div>
    <div class="card-body">
        {{ include('admin/quiz/_form.html.twig', {'button_label': 'Créer'}) }}
    </div>
</div>

<a href="{{ path('admin_quiz_index') }}" class="btn-secondary" style="display: inline-block; margin-top: 1rem; text-decoration: none; padding: 0.5rem 1rem; border-radius: 6px;">
    Retour à la liste
</a>
>>>>>>> user
{% endblock %}
