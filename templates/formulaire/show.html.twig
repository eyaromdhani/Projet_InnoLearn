{% extends 'base.html.twig' %}

{% block title %}Formulaire{% endblock %}

{% block body %}
    <h1>Formulaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formulaire.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ formulaire.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ formulaire.description }}</td>
            </tr>
            <tr>
                <th>TempsLimite</th>
                <td>{{ formulaire.tempsLimite }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ formulaire.category }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_formulaire_index') }}">back to list</a>

    <a href="{{ path('app_formulaire_edit', {'id': formulaire.id}) }}">edit</a>

    {{ include('formulaire/_delete_form.html.twig') }}
{% endblock %}
